#include "../headers/protocoles.h"

void call_dns(int verbosite)
{
    if (verbosite == 3 || verbosite == 2)
    {
        printf("Protocol DNS (Not implemented)\n");
    }

    else
    {
        printf(" DNS\n");
    }
}